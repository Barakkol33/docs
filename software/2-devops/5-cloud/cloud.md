# Cloud

Cloud = renting compute + storage + managed services.

## AWS 

- IAM (permissions)
- VPC (networking)
- ECR (Docker image registry)
- EC2 (virtual machines)
- EKS (managed Kubernetes)
- S3 (object storage)
- Fargate (run containers without managing servers)

- GCP, Azure (similar building blocks with different names)
